# This file is sourced only for login shell (i.e. shells invoked with "-" as the first character of argv[0], and shells invoked with the -l flag.).
# It should contain commands that should be executed only in login shells. 
# It should be used to set the terminal type and run a series of external commands (fortune, msgs, from, etc.).
