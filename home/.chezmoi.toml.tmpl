{{- $useBitwarden := promptBoolOnce . "useBitwarden" "Bitwarden で管理しているシークレットを適用しますか" false -}}

[data]
    useBitwarden = {{ $useBitwarden }}

{{- if $useBitwarden }}

[bitwarden]
    unlock = "auto"
{{- end }}
